# API dependencies for Copolymerization Prediction
# Install with: pip install -r requirements.txt

# Core API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# HTTP client (for testing)
requests>=2.31.0

# Optional: production server
gunicorn>=21.2.0

# Core ML dependencies (if not already installed)
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# Chemistry and molecular processing
rdkit-pypi>=2023.3.1  # RDKit for molecular descriptors and SMILES processing
morfeus-ml>=0.7.0     # Morfeus for quantum chemical calculations (monomer features)
qcengine>=0.27.0      # Quantum chemistry engine (required by morfeus)
qcelemental>=0.26.0   # Quantum chemistry element data (required by morfeus)
                      # Note: Requires xtb binary for full functionality

# Chemical name to SMILES conversion
pubchempy>=1.0.4      # PubChem API client for chemical name resolution
diskcache>=5.6.0      # Disk-based caching for name-to-SMILES conversions
backoff>=2.2.0        # Exponential backoff for API retries

# Note: morfeus requires additional system dependencies:
# - xtb (extended tight-binding) binary
# - qcengine and qcelemental for quantum chemistry calculations
# Install xtb: conda install -c conda-forge xtb
# Or see: https://github.com/grimme-lab/xtb

